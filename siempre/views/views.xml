<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="siempre.list">
      <field name="name">siempre list</field>
      <field name="model">siempre.siempre</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="siempre.action_window">
      <field name="name">siempre window</field>
      <field name="res_model">siempre.siempre</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="siempre.action_server">
      <field name="name">siempre server</field>
      <field name="model_id" ref="model_siempre_siempre"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="siempre" id="siempre.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="siempre.menu_1" parent="siempre.menu_root"/>
    <menuitem name="Menu 2" id="siempre.menu_2" parent="siempre.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="siempre.menu_1_list" parent="siempre.menu_1"
              action="siempre.action_window"/>
    <menuitem name="Server to list" id="siempre" parent="siempre.menu_2"
              action="siempre.action_server"/>
    -->
  </data>
</odoo>