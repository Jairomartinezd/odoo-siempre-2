<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="indicadores.list">
      <field name="name">indicadores list</field>
      <field name="model">indicadores.indicadores</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="indicadores.action_window">
      <field name="name">indicadores window</field>
      <field name="res_model">indicadores.indicadores</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="indicadores.action_server">
      <field name="name">indicadores server</field>
      <field name="model_id" ref="model_indicadores_indicadores"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="indicadores" id="indicadores.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="indicadores.menu_1" parent="indicadores.menu_root"/>
    <menuitem name="Menu 2" id="indicadores.menu_2" parent="indicadores.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="indicadores.menu_1_list" parent="indicadores.menu_1"
              action="indicadores.action_window"/>
    <menuitem name="Server to list" id="indicadores" parent="indicadores.menu_2"
              action="indicadores.action_server"/>
    -->
  </data>
</odoo>